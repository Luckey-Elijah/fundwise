name: app
version: 0.0.1
publish_to: "none"
description: Open source budgeting.
environment: { sdk: ">=3.5.1 <4.0.0" }

dependencies:
  bloc: ^8.1.4
  dart_mappable: ^4.2.2
  duck_router: ^4.0.0
  flailwind: { path: ./flailwind }
  flutter: { sdk: flutter }
  flutter_adaptive_scaffold: ^0.2.0
  flutter_bloc: ^8.1.6
  flutter_gutter: ^2.0.0
  intl: ^0.19.0
  pocketbase: ^0.18.1
  shared_preferences: ^2.2.3
  stream_transform: ^2.1.0

dev_dependencies:
  build_runner: ^2.4.11
  dart_mappable_builder: ^4.2.3
  flutter_test: { sdk: flutter }
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
