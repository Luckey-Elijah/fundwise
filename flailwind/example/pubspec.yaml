name: example
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1
environment: { sdk: ">=3.4.3 <4.0.0" }

dependencies:
  flailwind: { path: ../ }
  flutter: { sdk: flutter }

dev_dependencies: { flutter_lints: ^3.0.0 }
flutter: { uses-material-design: true }
